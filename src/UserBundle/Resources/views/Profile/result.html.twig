{% extends "UserBundle::layout.html.twig" %}

{% block body %}

    {% for user in listUsers %}
        <a href="{{ path('user_find', {'id': user.id}) }}">
            {{ user.username }}
            {{ user.city }}
        </a>
    {% endfor %}

{% endblock %}